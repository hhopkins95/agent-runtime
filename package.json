{
  "name": "agent-service-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace for agent runtime packages",
  "scripts": {
    "build": "pnpm -r build",
    "build:backend": "pnpm --filter @hhopkins/agent-runtime build",
    "build:client": "pnpm --filter @hhopkins/agent-runtime-react build",
    "check": "pnpm -r check",
    "clean": "pnpm -r exec rm -rf dist",
    "dev:backend": "pnpm --filter @hhopkins/agent-runtime dev",
    "dev:client": "pnpm --filter @hhopkins/agent-runtime-react dev",
    "example:backend": "pnpm --filter @example/backend dev",
    "example:frontend": "pnpm --filter @example/frontend dev",
    "dev": "concurrently --names \"backend,client,ex-backend,ex-frontend\" --prefix-colors \"blue,magenta,green,cyan\" \"pnpm dev:backend\" \"pnpm dev:client\" \"pnpm example:backend\" \"pnpm example:frontend\"",
    "example:dev": "pnpm run example:backend & pnpm run example:frontend"
  },
  "workspaces": [
    "backend",
    "client",
    "example/backend",
    "example/frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
